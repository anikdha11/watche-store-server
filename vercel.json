{
  "version": 2,
  "builds": [
    {
      "src": "./index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" },
    { "src": "/products", "methods": ["POST"], "dest": "/products" },
    { "src": "/users", "methods": ["POST"], "dest": "/users" }
  ]
}
